# v1.9.8 - 2026-05-19 - Widget Render

> **Widget Render** — fixes the booking widget crashing silently in the browser due to `process.env.NODE_ENV` shipping as a live runtime reference in the UMD bundle. Rebuilt the bundle with the value inlined at build time, added a visible offline fallback, and applied dark-theme CSS overrides so the widget is readable on the site's dark surface.

---

## What's Changed

### Fixed
- `assets/booking-widget.js` — rebuilt with `process.env.NODE_ENV` replaced at build time; widget no longer throws `ReferenceError: process is not defined` on load; bundle size reduced from 483 kB to 154 kB (dev React excluded)
- `assets/booking-widget.css` — rebuilt from latest widget source; dark-theme CSS overrides prepended so all widget states (loading, slots, error, forms) are visible on the site's `#12121e` surface
- `index.html` — `data-api-url` restored to `http://74.208.9.49:3001` (had been temporarily set to `localhost:3000` during local testing); `data-service-id` and `data-location-id` updated to current database seed values

### Added
- `index.html` — `.bw-fallback` div with CSS: displays "Online booking is loading… call (919) 555-1234 or walk in" when the booking API is unreachable, replacing the blank box users previously saw

> 3 files changed — `index.html`, `assets/booking-widget.js`, `assets/booking-widget.css`

---

## Snapshot

| Field | Value |
|---|---|
| Tag | `v1.9.8__widget-render__commit-2975d62` |
| Commit | `2975d62` |
| Branch | `main` |
| Date | 2026-05-19 |
| Author | CyberAntAI |

---

## Full Changelog

- [`2975d62`](../../commit/2975d62) — fix(booking): rebuild widget bundle, add offline fallback, restore VPS API URL

**Previous release:** [`82bf7c7`](../../commit/82bf7c7) — v1.9.7 Full Context
